var settings = new Settings();
var ui = new UI();


function setup(){
    createCanvas(settings.screenwidth,settings.screenheight);
}

function draw(){
    background(0);
    ui.draw();
}

function keyPressed(){
    
return
}
